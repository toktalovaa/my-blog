import React from "react";
import styles from "./Watches.module.css";

const Watches=() => {
    return(
        <div>
        <h1 className={styles.watches}>Здесь макбуки</h1>
        </div>
    )
}

export default Watches;